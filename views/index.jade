extends layout

block content
    #header
    #app
    #footer


    h1= title
    p Welcome to #{title}

    if user
        p id: #{user._id}
        p user: #{user.username}
        p email: #{user.email}
        p admin: #{user.admin}

    hr

    div(ng-view)
